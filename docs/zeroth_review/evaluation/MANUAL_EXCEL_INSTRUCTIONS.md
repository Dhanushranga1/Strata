# 📝 Manual Step Required: Create Excel Evaluation Rubric

**File:** `evaluation.xlsx`  
**Location:** `TicketPilot_Zeroth_Review/evaluation/`  
**Status:** ⚠️ **REQUIRES MANUAL CREATION** (Python environment issue prevented automation)

---

## Why Manual Creation is Needed

The automated Excel file creation script encountered a Python virtual environment issue. While the `evaluation/rubric.csv` file has been created with all rubric data, the Excel file with **automated decision formulas** needs to be created manually.

---

## How to Create evaluation.xlsx

### Option 1: Using Microsoft Excel / LibreOffice Calc

1. **Open `rubric.csv`** in Excel or LibreOffice Calc
2. **Create two worksheets:**
   - Sheet 1: "Rubric" (paste rubric data)
   - Sheet 2: "ScoringGuide" (create scoring reference)

3. **Rubric Sheet Structure:**
   - **Row 1 (Headers):** `CO_Range | CriterionID | CriterionName | Category | Description | MaxScore | ScoreGiven | Comments`
   - **Rows 2-6 (Criteria):** Copy data from `rubric.csv`
   - **Row 8:** Add summary: `Total IA Score: =SUMIFS(G:G,D:D,"IA")`
   - **Row 9:** Add summary: `Total GA Score: =SUMIFS(G:G,D:D,"GA")`
   - **Row 11:** Add decision: `Decision: =IF(SUMIFS(G:G,D:D,"IA")>=5,"Accepted",IF(AND(SUMIFS(G:G,D:D,"GA")>2.5,SUMIFS(G:G,D:D,"GA")<5),"Accepted with Revision","Rejected"))`

4. **ScoringGuide Sheet Structure:**
   - **Row 1 (Headers):** `Performance Level | Numeric Score | Description`
   - **Rows 2-6:**
     - Excellent | 2 | Outstanding work; exceeds all expectations
     - Good | 1.5 | Very good work; meets all expectations with minor improvements
     - Satisfactory | 1 | Acceptable work; meets minimum requirements
     - Needs Improvement | 0.5 | Below expectations; requires substantial revision
     - Unsatisfactory | 0 | Does not meet requirements; unacceptable quality

5. **Format the Rubric:**
   - Bold header row (row 1)
   - Apply blue fill (#4472C4) to header row
   - Center-align headers
   - Set column widths:
     - CO_Range: 12
     - CriterionID: 12
     - CriterionName: 60
     - Category: 10
     - Description: 80
     - MaxScore: 10
     - ScoreGiven: 12
     - Comments: 40

6. **Save as `evaluation.xlsx`** in `TicketPilot_Zeroth_Review/evaluation/`

---

### Option 2: Using Python Script (If Environment is Fixed)

If you have a working Python environment with `openpyxl` installed, you can run the pre-created script:

```bash
cd TicketPilot_Zeroth_Review/evaluation
python3 ../create_evaluation.py
```

**Script Location:** `TicketPilot_Zeroth_Review/create_evaluation.py`

**Required Package:**
```bash
pip install openpyxl
```

---

## Verification Checklist

After creating `evaluation.xlsx`, verify:

- [ ] **Two worksheets exist:** "Rubric" and "ScoringGuide"
- [ ] **All 5 criteria are listed** with correct Category (IA/GA)
- [ ] **Formulas work correctly:**
  - [ ] Total IA Score calculates (should be blank until ScoreGiven is filled)
  - [ ] Total GA Score calculates (should be blank until ScoreGiven is filled)
  - [ ] Decision formula displays correct logic
- [ ] **Scoring guide is complete** (5 performance levels)
- [ ] **File size is <100KB**

---

## Test the Formulas

1. **Enter sample scores** in Column G (ScoreGiven):
   - Problem Identification (IA): 2
   - Literature Survey (IA): 2
   - Presentation (IA): 1.5
   - Objectives (GA): 2
   - SDG Alignment (GA): 2

2. **Expected Results:**
   - Total IA Score: **5.5** (2 + 2 + 1.5)
   - Total GA Score: **4.0** (2 + 2)
   - Decision: **"Accepted"** (IA >= 5)

3. **Test Edge Cases:**
   - **Accepted with Revision:** IA = 4, GA = 3.5 → Should show "Accepted with Revision"
   - **Rejected:** IA = 3, GA = 2 → Should show "Rejected"

---

## Alternative: CSV-Only Evaluation

If Excel creation is not feasible, evaluators can use the **CSV file** (`rubric.csv`) directly:

1. Open `rubric.csv` in Google Sheets or Excel
2. Manually fill in Column 7 (ScoreGiven)
3. Manually calculate:
   - Total IA = Sum of scores for IA criteria
   - Total GA = Sum of scores for GA criteria
4. Apply decision logic:
   - IF IA >= 5 → "Accepted"
   - ELSE IF (GA > 2.5 AND GA < 5) → "Accepted with Revision"
   - ELSE → "Rejected"

---

## Reference Documents

- **Rubric Data (CSV):** `evaluation/rubric.csv`
- **Evaluation Guide:** `evaluation/evaluation_summary.md`
- **Python Script:** `create_evaluation.py` (in root of Zeroth_Review folder)

---

**Last Updated:** 2025-11-09T00:00:00Z  
**Auto-generated by agent**
